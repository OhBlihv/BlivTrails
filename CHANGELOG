Update ParticleEffect Library

Changes from @confuser on Github
- Change from BoneCP to ormlite SQL
- Back item material is now configurable
- Support for 1.7 servers
- Other changes

Add default colour to note trail

Fixed (/trailadmin particles) having different particle names to (/trailadmin add)

Fix MobSpell trail overlapping with another trail in the default config

Fix space at the beginning of each printed message (even without prefix)

Fix console error when an uncolourable trail attempts to find its colour value in the config

Fix trail not removing on disconnect when using MySQL

Re-arrange of the messages.yml
- New Section: messages.no-permission
	- See the updated config.yml on the Spigot page for more information

New Permissions:
	blivtrails.options.type
	blivtrails.options.type.trace
	blivtrails.options.type.random
	blivtrails.options.type.dynamic
	
	blivtrails.options.length
	blivtrails.options.length.short
	blivtrails.options.length.medium
	blivtrails.options.length.long
	
	blivtrails.options.height
	blivtrails.options.height.feet
	blivtrails.options.height.waist
	blivtrails.options.height.halo
	
	blivtrails.options.colour
	blivtrails.options.colour.<colour> (Grab the full list from the config)